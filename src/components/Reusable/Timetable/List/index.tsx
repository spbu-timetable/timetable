import React from "react";

type Props = {
  // items: any;
  // startDate: Date;
  // endDate: Date;
  // getCabinetTimetable: (
  //   cabinetIndex: number,
  //   cabinetOid: string,
  //   startDate: Date,
  //   endDate: Date
  // ) => void;
};

const TimetableList = (props: Props) => {
  return <div>list</div>;
};

export default TimetableList;
